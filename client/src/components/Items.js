import React from 'react';

function Items(props) {

    return (
        <div>
            {/* <button  dataid={item.id} key={item.id} item={item.itemname} className="btn btn-light" style={props.style}  >{item.itemname}</button> */}
            {/* <h3>{props.items}</h3> */}
        </div>
    )

}
export default Items;